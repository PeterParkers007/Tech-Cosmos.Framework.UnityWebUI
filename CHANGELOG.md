# Changelog

## [1.0.0] - 2025-06-24

### Added
- UPM package layout (`com.unitywebui.core`)
- Windows GPU WebView2 backend with Editor preview
- Action Mapper: HTML button scan + UnityEvent binding
- `SetPageVisible` / `WebViewPageToggle` for ESC-style menus
- Package path resolution for Git/local install
- Bridge script in `Resources` for runtime builds

### Notes
- Build GPU native plugin before first use: **Window → Unity Web UI → Build Windows GPU Plugin**
